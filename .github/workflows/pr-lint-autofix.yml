name: PR自動修正（Copilot連携）

on:
    # PRに「@fix」とコメントすることでトリガーする
    # issue_commentのactorはコメントした人間（write権限あり）になるため
    # codex-actionのcollaboratorチェックを確実に通過できる
    issue_comment:
        types: [created]

permissions:
    contents: write
    pull-requests: write

jobs:
    # ──────────────────────────────────────────────
    # ジョブ1: 前準備
    # PRへの「@fix」コメントのみを対象とし、head_refをAPIで取得して後続ジョブに渡す
    # ──────────────────────────────────────────────
    prepare:
        runs-on: ubuntu-latest
        # PRへのコメントかつ「@fix」を含む場合のみ実行する
        if: |
            github.event.issue.pull_request != null &&
            contains(github.event.comment.body, '@fix')
        outputs:
            head_ref: ${{ steps.pr-info.outputs.head_ref }}
            pr_number: ${{ steps.pr-info.outputs.pr_number }}
        steps:
            # issue_commentイベントにはhead.refが含まれないためAPIで取得する
            - name: PRのブランチ情報を取得
              id: pr-info
              uses: actions/github-script@v7
              with:
                  script: |
                      const prNumber = context.issue.number;
                      const { data: pr } = await github.rest.pulls.get({
                        owner: context.repo.owner,
                        repo: context.repo.repo,
                        pull_number: prNumber,
                      });
                      core.setOutput('pr_number', String(prNumber));
                      core.setOutput('head_ref', pr.head.ref);

            - name: PRブランチをチェックアウト
              uses: actions/checkout@v4
              with:
                  ref: ${{ steps.pr-info.outputs.head_ref }}

            - name: Node.js 24をセットアップ
              uses: actions/setup-node@v4
              with:
                  node-version: '24'
                  cache: 'npm'

            # npm ciを実行してキャッシュを作成し後続ジョブのインストールを高速化する
            - name: 依存関係をインストール
              run: npm ci

    # ──────────────────────────────────────────────
    # ジョブ2: ESLint静的解析（prepareの後、knipと並列実行）
    # ──────────────────────────────────────────────
    lint:
        runs-on: ubuntu-latest
        needs: [prepare]
        steps:
            - name: PRブランチをチェックアウト
              uses: actions/checkout@v4
              with:
                  ref: ${{ needs.prepare.outputs.head_ref }}

            - name: Node.js 24をセットアップ
              uses: actions/setup-node@v4
              with:
                  node-version: '24'
                  cache: 'npm'

            - name: 依存関係をインストール
              run: npm ci

            # エラーがあっても後続ステップに進むためcontinue-on-error: trueを設定
            - name: ESLintを実行（JSON出力）
              continue-on-error: true
              run: npm run lint

            # autofixジョブで参照するためアーティファクトとして保存
            - name: ESLint結果をアーティファクトとして保存
              uses: actions/upload-artifact@v4
              with:
                  name: eslint-results
                  path: eslint_results.json
                  if-no-files-found: warn

    # ──────────────────────────────────────────────
    # ジョブ3: knip未使用コード解析（lintと並列実行）
    # ──────────────────────────────────────────────
    knip:
        runs-on: ubuntu-latest
        needs: [prepare]
        steps:
            - name: PRブランチをチェックアウト
              uses: actions/checkout@v4
              with:
                  ref: ${{ needs.prepare.outputs.head_ref }}

            - name: Node.js 24をセットアップ
              uses: actions/setup-node@v4
              with:
                  node-version: '24'
                  cache: 'npm'

            - name: 依存関係をインストール
              run: npm ci

            # 未使用コードが検出されても後続ステップに進むためcontinue-on-error: trueを設定
            - name: knipを実行（JSON出力）
              continue-on-error: true
              run: npm run knip

            # autofixジョブで参照するためアーティファクトとして保存
            - name: knip結果をアーティファクトとして保存
              uses: actions/upload-artifact@v4
              with:
                  name: knip-results
                  path: knip_results.json
                  if-no-files-found: warn

    # ──────────────────────────────────────────────
    # ジョブ4: Copilotレビュー取得 → 自動修正 → PR解決済み更新
    # ──────────────────────────────────────────────
    autofix:
        runs-on: ubuntu-latest
        needs: [prepare, lint, knip]
        steps:
            # pushできるようfetch-depth: 0でフルクローン
            - name: PRブランチをチェックアウト
              uses: actions/checkout@v4
              with:
                  ref: ${{ needs.prepare.outputs.head_ref }}
                  fetch-depth: 0

            - name: Node.js 24をセットアップ
              uses: actions/setup-node@v4
              with:
                  node-version: '24'
                  cache: 'npm'

            - name: 依存関係をインストール
              run: npm ci

            # lint/knipジョブで生成したJSONファイルをワーキングディレクトリに配置
            - name: ESLint結果をダウンロード
              uses: actions/download-artifact@v4
              with:
                  name: eslint-results
              continue-on-error: true

            - name: knip結果をダウンロード
              uses: actions/download-artifact@v4
              with:
                  name: knip-results
              continue-on-error: true

            # CopilotのレビューをAPIで取得する（issue_commentイベントにはreview情報がないため）
            # REST APIでレビュー本文・インラインコメントを取得し、GraphQL APIでスレッド詳細も取得する
            - name: Copilotのレビューコメントを取得
              uses: actions/github-script@v7
              with:
                  script: |
                      const fs = require('fs');
                      const prNumber = parseInt('${{ needs.prepare.outputs.pr_number }}');

                      // レビュー本文（REST）
                      const { data: reviews } = await github.rest.pulls.listReviews({
                        owner: context.repo.owner, repo: context.repo.repo, pull_number: prNumber,
                      });
                      const copilotReview = reviews
                        .filter(r => r.user.login.toLowerCase().includes('copilot'))
                        .at(-1);

                      // インラインコメント（REST）
                      const { data: comments } = await github.rest.pulls.listReviewComments({
                        owner: context.repo.owner, repo: context.repo.repo, pull_number: prNumber,
                      });
                      const copilotComments = comments.filter(c =>
                        c.user.login.toLowerCase().includes('copilot')
                      );

                      // codex用テキストファイル生成
                      let reviewText = '## GitHub Copilot レビュー本文\n';
                      reviewText += (copilotReview?.body ?? '') + '\n\n';
                      reviewText += '## GitHub Copilot インラインコメント\n';
                      for (const c of copilotComments) {
                        reviewText += `ファイル: ${c.path} 行${c.line ?? c.original_line}: ${c.body}\n`;
                      }
                      fs.writeFileSync('/tmp/copilot_review.txt', reviewText);

                      // スレッド詳細JSON（GraphQL）：後続の個別分析ステップで使用
                      const result = await github.graphql(`
                        query($owner: String!, $repo: String!, $prNumber: Int!) {
                          repository(owner: $owner, name: $repo) {
                            pullRequest(number: $prNumber) {
                              reviewThreads(first: 100) {
                                nodes {
                                  id isResolved path line originalLine
                                  comments(first: 1) {
                                    nodes { author { login } body }
                                  }
                                }
                              }
                            }
                          }
                        }
                      `, { owner: context.repo.owner, repo: context.repo.repo, prNumber });

                      const threads = result.repository.pullRequest.reviewThreads.nodes
                        .filter(n =>
                          !n.isResolved &&
                          n.comments.nodes[0]?.author?.login?.toLowerCase().includes('copilot')
                        )
                        .map(n => ({
                          id: n.id,
                          path: n.path,
                          line: n.line ?? n.originalLine,
                          body: n.comments.nodes[0].body,
                        }));

                      fs.writeFileSync('/tmp/copilot_threads.json', JSON.stringify(threads));
                      console.log(`取得したCopilotスレッド数: ${threads.length}`);

            # CLAUDE.md・Copilotレビュー・ESLint・knipの結果を統合した修正プロンプトを生成する
            - name: 自動修正プロンプトを生成
              run: |
                  {
                    echo "あなたはTypeScript/AWS CDKの専門家です。"
                    echo "以下の開発規約（copilot-instructions.md）に従い、コードレビュー・静的解析の結果のうち"
                    echo "「対応が必要」と判断したものだけを修正してください。"
                    echo ""
                    echo "## 修正の判断基準（必ず守ること）"
                    echo "- ESLintのエラー（severity=2）は必ず修正する"
                    echo "- ESLintの警告（severity=1）は、CLAUDE.mdの規約に明確に違反している場合のみ修正する"
                    echo "- knipの未使用コードは、DRY原則・単一責任の原則に違反している場合のみ修正する"
                    echo "- Copilotのレビューコメントは、開発規約に違反している指摘のみ修正する"
                    echo "- 動作に影響しない軽微な問題・好みの問題は修正しない"
                    echo "- AWS CDKコンストラクタの構造・動作は変えない"
                    echo ""
                    echo "## 開発規約(copilot-instructions.md)"
                    cat ./github/copilot-instructions.md
                    echo ""
                    echo "## GitHub Copilot コードレビュー結果"
                    cat /tmp/copilot_review.txt 2>/dev/null || echo "（Copilotのレビュー結果なし）"
                    echo ""
                    echo "## ESLint静的解析結果（JSON）"
                    cat eslint_results.json 2>/dev/null || echo "（ESLint結果なし）"
                    echo ""
                    echo "## knip未使用コード解析結果（JSON）"
                    cat knip_results.json 2>/dev/null || echo "（knip結果なし）"
                  } > /tmp/fix-prompt.txt

            # actorはコメントした人間（write権限あり）のためcollaboratorチェックを通過する
            - name: OpenAI Codex（gpt-5）でコード自動修正
              uses: openai/codex-action@v1
              with:
                  openai-api-key: ${{ secrets.OPENAI_API_KEY }}
                  model: gpt-5
                  prompt-file: /tmp/fix-prompt.txt
                  safety-strategy: drop-sudo
                  output-file: /tmp/codex-output.txt

            # 変更があった場合のみコミット・push（[skip ci]でこのワークフローの再トリガーを防止）
            # SHAを後続ステップのコメントで使用するためoutputとして出力する
            - name: 修正をコミットしてpush
              id: commit-push
              run: |
                  git config user.name "github-actions[bot]"
                  git config user.email "github-actions[bot]@users.noreply.github.com"
                  git add -A
                  if git diff --cached --quiet; then
                    echo "修正対象の変更はありませんでした"
                    echo "sha=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT
                  else
                    git commit -m "fix: Copilot/ESLint/knipによる自動修正 [skip ci]"
                    git push origin HEAD
                    echo "sha=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT
                  fi

            # git diffとCopilotコメントをOpenAI APIで照合し、コメントごとに対応状況を判定する
            - name: 各Copilotコメントへの対応状況をAIで分析
              env:
                  OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
              run: |
                  # 修正コミットのdiffを取得（変更なしの場合は空文字）
                  GIT_DIFF=$(git diff HEAD~1 HEAD 2>/dev/null || echo "")
                  THREADS=$(cat /tmp/copilot_threads.json 2>/dev/null || echo "[]")

                  # スレッドが0件の場合はスキップ
                  if [ "$(echo "$THREADS" | jq 'length')" -eq 0 ]; then
                    echo "[]" > /tmp/comment_resolutions.json
                    exit 0
                  fi

                  # jqでAPIリクエストボディを組み立てる（シェル変数の特殊文字をエスケープ）
                  jq -n \
                    --arg diff "$GIT_DIFF" \
                    --argjson threads "$THREADS" \
                    '{
                      model: "gpt-4o",
                      response_format: {type: "json_object"},
                      messages: [{
                        role: "user",
                        content: (
                          "以下のgit diffとGitHub Copilotのレビューコメント一覧を照合し、"
                          + "各コメントがこのコミットで対応されたかを判定してください。\n\n"
                          + "判定基準:\n"
                          + "- diffにコメントで指摘された問題への対応変更がある → addressed: true\n"
                          + "- 対応する変更がない/無関係な変更のみ → addressed: false\n"
                          + "- reasonは日本語で具体的に記載すること\n\n"
                          + "出力形式（コードブロックなしのJSONオブジェクト）:\n"
                          + "{\"<スレッドID>\": {\"addressed\": true/false, \"reason\": \"理由\"}, ...}\n\n"
                          + "## Git Diff\n" + $diff + "\n\n"
                          + "## Copilotコメント一覧（JSON）\n" + ($threads | tostring)
                        )
                      }]
                    }' > /tmp/analysis-request.json

                  # OpenAI APIを呼び出し、JSONレスポンスを保存
                  curl -s https://api.openai.com/v1/chat/completions \
                    -H "Content-Type: application/json" \
                    -H "Authorization: Bearer $OPENAI_API_KEY" \
                    -d @/tmp/analysis-request.json \
                    | jq '.choices[0].message.content | fromjson' > /tmp/comment_resolutions.json

            # 各CopilotスレッドにAI分析結果を元に個別返信し、対応済みのみ解決済みに更新する
            - name: Copilotコメントに個別返信して選択的に解決済み更新
              uses: actions/github-script@v7
              env:
                  COMMIT_SHA: ${{ steps.commit-push.outputs.sha }}
              with:
                  script: |
                      const fs = require('fs');

                      const threads = JSON.parse(fs.readFileSync('/tmp/copilot_threads.json', 'utf8'));
                      const resolutions = JSON.parse(
                        fs.readFileSync('/tmp/comment_resolutions.json', 'utf8')
                      );
                      const commitSha = process.env.COMMIT_SHA ?? '（コミットなし）';

                      for (const thread of threads) {
                        const res = resolutions[thread.id] ?? {
                          addressed: false, reason: '分析結果が取得できませんでした',
                        };

                        const comment = res.addressed
                          ? `🤖 **自動修正ツールによる対応（対応済み）**\n\n` +
                            `この指摘はOpenAI Codex（gpt-5）による自動修正で対応しました。\n\n` +
                            `**対応内容:** ${res.reason}\n\n` +
                            `**対応コミット:** ${commitSha}`
                          : `🤖 **自動修正ツールによる確認（未対応）**\n\n` +
                            `この指摘は今回の自動修正では対応しませんでした。\n\n` +
                            `**理由:** ${res.reason}\n\n` +
                            `*CLAUDE.mdの開発規約に基づき、対応不要と判断した指摘は修正していません。手動での確認をお願いします。*`;

                        await github.graphql(`
                          mutation($threadId: ID!, $body: String!) {
                            addPullRequestReviewThreadReply(input: {
                              pullRequestReviewThreadId: $threadId, body: $body
                            }) { comment { id } }
                          }
                        `, { threadId: thread.id, body: comment });

                        if (res.addressed) {
                          await github.graphql(`
                            mutation($threadId: ID!) {
                              resolveReviewThread(input: { threadId: $threadId }) { thread { isResolved } }
                            }
                          `, { threadId: thread.id });
                          console.log(`対応済み・解決済みに更新: ${thread.id}`);
                        } else {
                          console.log(`未対応・resolveしない: ${thread.id}`);
                        }
                      }
